import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saket.app',
  appName: 'Daily Motivational Quotes ',
  webDir: 'build'
};

export default config;
